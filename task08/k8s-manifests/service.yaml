apiVersion: v1
kind: Service
metadata:
  name: ${service_name}
  namespace: ${namespace}
  labels:
    app: ${app_name}
spec:
  type: LoadBalancer
  selector:
    app: ${app_name}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: ${container_port}
